# 2026-08-19 商家宝：模块5 AI智能创作集成（素材库 + 作品库 + 一键追爆）

## 完成内容（Task #18-#23 全部完成）

### 新建素材管家服务 (Task #18)
- `app/services/material_library.py`: 分组管理 + 4类素材（text/image/audio/video）CRUD + AI爆款文案生成
- 存储路径：`storage/material_library.json`（索引）+ `storage/materials/`（文件）
- `generate_ai_copy()` 复用 `llm._generate_response`，走当前配置的 LLM Provider

### 新建发布任务调度服务 (Task #20)
- `app/services/publish_tasks.py`: 创建发布任务 + 后台守护线程调度 + 重启恢复
- 状态机：pending → publishing → published/failed
- 5秒轮询，到期时调用 `china_publish.upload_video`
- 重启时自动将 publishing 状态标记为 failed

### Main.py 素材库 & 作品库页面 (Task #19)
- 新增 PAGE_MATERIAL / PAGE_LIBRARY 路由常量
- 顶部导航新增"作品库"和"素材库"两个入口
- 素材库页面：AI爆款文案生成 + 分组管理 + 四类素材Tab + CRUD + "用于视频生成"快捷入口
- 作品库页面：我的作品（成片预览/再次生成/发布/删除）+ 发布任务中心（创建任务/定时发布/状态徽章/重试/删除）
- 修复 `FILE_EXTENSIONS` 导入 bug：原代码通过实例属性访问，改为模块级常量导入

### 一键追爆集成 (Task #21)
- 在视频生成页 `_render_script_settings` 中新增"Trending Copy Generator"expander
- 输入追爆主题 + 风格要求 + 最大字数 → AI生成爆款文案 → 自动填入文案框
- 与素材库页面的AI文案生成共用 `material_library_service.generate_ai_copy()`

### i18n 词条补全 (Task #22)
- 补齐 79 个缺失的 tr() key 到 zh.json 和 en.json
- zh.json Translation 对象现有 503 个 key，en.json 同步

### AppTest 验证 & 服务重启 (Task #23)
- 所有文件编译通过（py_compile）
- 服务导入和初始化测试通过
- AppTest 初始渲染 0 错误，i18n 生效（按钮标签"素材库"正确显示）
- Streamlit 服务已在 8501 端口重启，健康检查返回 ok
- 注意：AppTest 点击导航按钮时遇到 format_func 兼容性问题（Streamlit测试框架已知限制，非应用bug）

## 技术要点
- FILE_EXTENSIONS 是 material_library.py 的模块级常量，不能通过 material_library_service 实例访问
- zh.json/en.json 是嵌套结构：{"Language": "...", "Translation": {key: value}}
- stable_selectbox 的 format_func 在 AppTest 中有兼容性问题，但不影响实际运行

### 视频生成页右侧预览面板 (Task #24)
- 参考超级员工系统截图，将视频生成页从 4 列改为 3 列：
  - 左侧：文案设置 + 一键追爆
  - 中间：视频/音频/字幕/水印设置
  - 右侧：固定的视频预览面板
- 预览面板支持：选择作品、播放视频、发布、再次生成、打开文件夹、删除
- 新增 i18n key：Video Preview（视频预览）、No Preview Video（暂无预览视频）
- AppTest 验证通过（0 错误），服务已重启
