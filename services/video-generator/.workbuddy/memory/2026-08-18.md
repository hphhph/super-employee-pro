# 2026-08-18 商家宝定制：账户管理 + 数据中心 + 科技蓝主题

## 完成内容（Task #11-#17 全部完成）

### 品牌重塑 (Task #11)
- `webui/Main.py`: page_title/品牌标题/About 全部改为"商家宝"
- i18n: Moonshot 推广链接显示文字改为"商家宝专属链接"（URL 保持原样，是返佣链接）
- GitHub issues 链接保留原仓库地址（代码注释与链接不改）

### 科技蓝主题 (Task #12)
- `webui/.streamlit/config.toml`: 新增 [theme] 段 — primaryColor #2563eb, bg #f7fafd
- `webui/styles.css`: 追加"商家宝科技蓝主题"段 — 品牌名渐变蓝文字、主按钮渐变+阴影、
  次级按钮蓝描边、容器蓝边框+淡蓝底、tab/checkbox/slider/进度条/表格统一科技蓝、暗色模式适配

### 视频模板汉化 (Task #13)
- 模板选择器用 tr() 词条（"Template Custom"="自定义"等 7 项），zh.json 已有全部翻译
- 补齐 68 条缺失 i18n 词条（导航/登录/账户/数据中心/验证码/平台筛选等）

### 片段时长选项 (Task #14)
- Main.py 片段时长下拉: options=[10,30,60,120]，format_func 显示 10秒/30秒/1分钟/2分钟
- schema.py: video_clip_duration default=10, le=120

### 设置栏报错修复 (Task #15)
- **根因**: `_CN_PLATFORM_FIELDS` 中字段 type 用了 "text"，st.text_input 只接受 "default"/"password"
  → StreamlitAPIException，导致设置弹窗崩溃
- **修复**: 4 处 "text" → "default"
- 另修复 `tr()` 用 `st.session_state["ui_language"]` 直接索引的脆弱性 → 改 `.get()` 安全读取

### 账户管理 (Task #16)
- `app/services/account.py`: JSON 存储(storage/accounts.json)、手机验证码登录(演示码123456)、
  微信 OpenID 登录、会话 token、子账号创建/权限分配(7种权限)/删除、默认管理员(13800000000)
- Main.py: 登录页(两个 tab)、账户管理页(子账号+权限分配)、顶部栏登录/登出按钮

### 数据中心 (Task #17)
- `app/services/analytics.py`: DailyMetrics(粉丝/播放/评论/点赞/分享/主页访问)、
  按日期+平台查询、聚合、昨日环比、CSV 导出(UTF-8 BOM)、演示数据播种
- Main.py: 日期范围+平台筛选、6 张指标卡片(蓝色渐变风格)、趋势折线图、明细表、导出按钮

### 页面路由
- 顶部栏新增三个导航按钮：视频生成 / 账户 / 数据中心（session_state["current_page"] 分发）
- 登录会话键: `auth_token`；未登录访问账户/数据中心页自动显示登录页

## 验证方式（重要，可复用）
用项目自带 .venv 的 Streamlit AppTest 全页面回归测试：
```python
from streamlit.testing.v1 import AppTest
at = AppTest.from_file("webui/Main.py", default_timeout=120)
at.session_state["settings_dialog_open"] = True  # 或 current_page / auth_token / ui_language
at.run()
print([e.message for e in at.exception])  # 应为空
```
- 注意：session_state 必须在**首次 run 之前**设置，二次 run 会触发 AppTest 对 format_func 的重放 bug（非真实缺陷）
- 7 个场景全部通过：视频页(zh/en)、设置弹窗(zh/en)、登录页、账户页(admin)、数据中心(admin)

## 遗留事项
- config.toml 主题与 config.toml 配置改动需**重启 Streamlit** 才生效
- 手机验证码为演示码 123456，微信登录为 OpenID 手动输入占位，接入真实 SMS/OAuth 需替换 account.py 对应方法
- 数据中心目前只有零值演示数据，真实数据需通过 analytics_service.record() 接入平台 API
